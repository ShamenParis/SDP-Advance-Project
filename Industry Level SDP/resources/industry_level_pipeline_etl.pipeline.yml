# The main pipeline for industry_level_pipeline

resources:
  pipelines:
    industry_level_pipeline_etl:
      name: industry_level_pipeline_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/industry_level_pipeline_etl"

      libraries:
        - glob:
            include: ../src/industry_level_pipeline_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
